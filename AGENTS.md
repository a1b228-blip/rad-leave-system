# 佳里奇美醫院 放射診斷科線上預假系統 (2027) 專案藍圖

## 專案簡介
本專案為佳里奇美醫院放射診斷科量身打造之「2027 年度同仁線上預假與搶假管理系統」，符合《勞動基準法》第 37 條國定假日規定，支援跨裝置即時同步、名冊優先排序、下拉式預假同仁名冊、主管雙向公告連動、依員工編號精確查詢解鎖，以及主管跨年度授權與年度鎖定管控。

## 目前完成功能 Checklist
- [x] **國定假日自動連動**：依據《勞動基準法》對照 2027 年節日（元旦、小年夜、除夕、初一~初三、228、兒童/清明節、勞動節 5/1、端午節、中秋節、國慶日），顯示 `🚩 節日` 標籤與 0 人停搶名額。
- [x] **預假同仁下拉選單**：日曆格子改為 `<details class="booked-dropdown">` 折疊式選單（`已預假 (N 人) ▾`），保持畫面乾淨整潔。
- [x] **佳里奇美醫院放射診斷科正式名冊匯入**：依據《派班表》匯入全科 27 位同仁（含 ID、姓名、職務、120/180 小時年假與預設密碼）。
- [x] **主管編輯與刪除權限修復**：修正編輯姓名/年假/權限與刪除同仁不會被舊快取覆蓋之 Bug。
- [x] **明文密碼顯示**：主管編輯同仁 Modal 中的密碼改為 plain text 文字輸入框。
- [x] **名冊絕對排序引擎**：員工編號 `911050` 歸類為名冊第 1 位，主管角色（`admin`）歸類於上方。
- [x] **全科預假規範雙向連動**：頂部按鈕 `📜 預假規則說明` 彈窗供同仁隨時檢視，主管控制台提供 `✍️ 編輯與發布全科預假規範說明` 編輯器，100% 即時發布連動。
- [x] **依員工編號精確查詢與解鎖**：主管控制台告別全科假單平鋪，提供下拉選單或手動輸入員工編號，精準秀出該同仁的預假日期與一鍵強撤解鎖。
- [x] **主管跨年度授權開放**：主管可動態輸入未來年份（如 2028）授權開放預假，同仁可在已授權年度間自由切換。
- [x] **主管年度關閉/鎖定管控**：主管可隨時「關閉/鎖定」特定年度，鎖定時同仁端跳出紅色公告 Banner，搶假按鈕全數自動停用。
- [x] **雙重萬能登入校正**：不分大小寫、前後空格與型態，且支援萬能員工編號校正救援，登入無障礙通行。
- [x] **登入畫面醫療視覺強化**：移除「(2027 年度)」舊年份標題，嵌入高科技放射診斷科醫療影像背景圖 (`radiology_bg.jpg`)。
- [x] **主管控制台同仁查詢動態連動**：補齊 `change` 與 `input` 即時事件監聽器，選擇員工編號瞬間帶出個人假單明細。
- [x] **GitHub Pages 雲端雙專案部署**：一鍵建立 Git 倉庫並啟用 GitHub Pages，提供全平台連線網址 (`rad-leave-system` / `pharmacy-leave-system`)。
- [x] **手機／電腦雙端雙文字機制 (Dual RWD Text Spans)**：日曆每格與按鈕支援 `.desktop-text` 與 `.mobile-text` 雙端切換，解決手機端換行錯位問題。
- [x] **手機端 1~31 日與週日~週六垂直絕對對齊**：對齊星期標題與日期格子 gap 及欄位 `min-width: 0`，當月所有 31 個日期 100% 精準對齊。
- [x] **每月 15 號自動鎖定下個月自主預假**：當今日日期 `>= 15` 號，自動關閉下月份之同仁自主預假按鈕並顯示警告 Banner。
- [x] **主管控制台手動代同仁預假與特許強行預假**：主管可選取同仁與日期代掛假單，若名額已滿跳出 `此日預假已額滿，是否確定強行預假？` 確認視窗。
- [x] **公出／公假假別支援 (0 小時不扣年假)**：支援同仁自主預假彈窗與主管後台選擇「公出/公假」，不扣除同仁年假且 100% 佔用當天預假名額。
- [x] **主管控制台同仁名冊手動拖拉排序 (Drag & Drop)**：名冊最左側提供 `⣿` 拖拉把手，主管可拖拉自訂順序並全系統連動。
- [x] **一鍵匯出月預假表 Excel (.xlsx 檔名動態連動年月)**：按鈕更名為「匯出月預假表 Excel」，直接產生真實 `.xlsx` 檔名自動帶入當下年月並下載至本地端。

## 檔案結構
- `index.html`: 主應用程式 HTML 結構。
- `styles.css`: 系統 Vanilla CSS 設計系統與玻璃擬態樣式。
- `app.js`: 核心 JavaScript 全域狀態、DOM 控制與 LocalStorage 持久化邏輯。
- `radiology_leave_rules_2027.md`: 預假規範文字檔。
- `放射診斷科同仁年假正式名冊_2027.xlsx` / `.csv`: 官方名冊檔案。
